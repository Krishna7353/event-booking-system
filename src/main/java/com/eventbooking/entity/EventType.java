package com.eventbooking.entity;

public enum EventType {
    SEATED,     // movies — individual seat matrix
    ZONED,      // concerts/ground events — price zones
    GENERAL     // standup — single capacity counter
}